<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.eci.cvds.sampleprj.dao.mybatis.mappers.ClienteMapper">

    <select parameterType="map" id="consultarClientes" resultMap="ClienteResult">
		SELECT
    
            c.nombre,
            c.documento,
            c.telefono,
            c.direccion,
            c.email,
            c.vetado,
            
            i.id,
            i.nombre,
            i.descripcion,
            i.fechalanzamiento,
            i.tarifaxdia,
            i.formatorenta,
            i.genero,   
            
            ir.id,
            ir.fechainiciorenta,
            ir.fechafinrenta,
            
            ti.id,
            ti.descripcion 

            
        FROM VI_CLIENTES AS c 
        LEFT JOIN VI_ITEMRENTADO AS ir ON c.documento=ir.CLIENTES_documento 
        LEFT JOIN VI_ITEMS AS i ON ir.ITEMS_id=i.id 
        LEFT JOIN VI_TIPOITEM AS ti ON i.TIPOITEM_id=ti.id;
    </select>

    <resultMap id="ClienteResult" type="Cliente"></resultMap>
    
    <resultMap id="ItemRentadoResult" type="ItemRentado"></resultMap>

    <resultMap id="ItemResult" type="Item"></resultMap>

    <resultMap id="TipoItemResult" type="TipoItem"></resultMap>

</mapper>